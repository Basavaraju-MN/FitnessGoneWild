# Optional. When the frontend and backend are served by the same Render Web Service,
# leave this unset and the app will use /api automatically in production.
VITE_API_BASE_URL=http://localhost:4000/api
VITE_SITE_URL=http://localhost:5173
